package org.parser.examples.alpha;

public enum Type {
    PROGRAM, LABELED ,BRANCH, GOTO, ASSIGN, CALL, RETURN,
    ACCUMULATOR, ADDRESS, NUMBER, LABEL,
    PUSH, POP, STACK_OP,
    ADD, SUB, MUL, DIV, MOD,
    LEQ, GEQ, LE, GE, EQ
    // operations funktionieren mit push und stack_op
}
